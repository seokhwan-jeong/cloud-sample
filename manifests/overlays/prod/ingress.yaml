apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-sample-ingress
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
  - host: cloud-sample.82210889.ce.kubepia.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-sample
            port:
              number: 80